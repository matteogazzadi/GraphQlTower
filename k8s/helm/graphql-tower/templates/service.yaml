apiVersion: v1
kind: Service
metadata:
  name: {{ include "graphql-tower.fullname" . }}-api
  labels:
    {{- include "graphql-tower.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  type: {{ .Values.api.service.type }}
  ports:
    - port: {{ .Values.api.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "graphql-tower.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: api
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "graphql-tower.fullname" . }}-web
  labels:
    {{- include "graphql-tower.labels" . | nindent 4 }}
    app.kubernetes.io/component: web
spec:
  type: {{ .Values.web.service.type }}
  ports:
    - port: {{ .Values.web.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "graphql-tower.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: web
